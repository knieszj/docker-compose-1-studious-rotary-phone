version: '3'

services:

  client:
    build: client/
    depends_on:
      - server
    ports:
    - '3000:3000'

  server:
    build: server/
    depends_on:
      - database
    ports:
    - '3001:3001'

  database:
    image: mongo:latest
